{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "generate-templ",
            "type": "shell",
            "command": "templ generate",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
